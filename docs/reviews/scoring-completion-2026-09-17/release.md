# 评分修复生产发布记录

日期：2026-09-17。

最终结果：2026-09-17 15:07（香港/新加坡时间）生产验收完成，正式站已部署 `4db1b48903d9134f2dbba54186ee027ade0fde9c`，状态 READY。[评分页](https://quantedge-chi.vercel.app/?tab=scoring) · [PR #336](https://github.com/mingtaohuang617/quantedge/pull/336) · [生产 CI](https://github.com/mingtaohuang617/quantedge/actions/runs/35192333593)。完整模型验证仍未通过。

用户已明确要求继续完成并上线。本次发布软件修复和实验性评分说明，保留 3.2.0 评分权重；不发布预测有效性声明，不把研究样本写入生产数据库。

## 范围与基线

- 发布前生产提交：`16c417fea3bfe48bdf74978e19538a8eddf82af4`，Vercel 状态 READY。
- 已合并该主分支的最新手动日线快照功能，保留原有更新方式；评分输入与显示行情日期继续分别标注。
- 包含上一轮 `5ab1834` 的跨资产评分边界、部分产品观测、验证状态与移动端稳定性修复，以及先前历史证据研究工具。
- 追加 `6c1662c`：AI 解读端到端传递资产类型；缺失类型和非股票不得使用股票综合分解释；切换标的、权重或评分依据时，旧异步成功/失败结果均不能覆盖当前状态。
- 不修改支付、访问权限、数据表结构或评分权重。
- 评分解读接入现有 Vercel 本地 AI 分发器，复用同一认证、限流、并发限制和缓存设施；当前评分功能不再依赖 Render 是否完成同步部署。非股票和无效评分在读取缓存或调用模型前返回 400。上线冒烟用无效请求核验这一拒绝路径，不调用付费模型。

## 完成与未完成的界限

本文件记录软件发布。此前 acceptance.json 中的 `production_release_ready=false` 是完整数据与模型验收结论，并非本次软件修复的发布结果；它继续保留，不因上线而改为 true。

完整历史股票成员及退市数据、财务与公司行动证据、全量 ETF 产品历史面板、加密基本面与真实执行成本仍未齐备，六批研究验收尚未全部完成。详情与关闭条件见同目录 README.md。现有后期保留样本也不支持提高趋势权重。

## 验证

- 合并后后端非联网、非慢速回归 1,455 项通过，12 项按标记排除；新增接口测试覆盖 stock、crypto、index_etf 与缺失资产类型，模型调用已替换为测试函数，无付费调用。
- 前端 54 个文件、788 项测试通过；新增 7 项资产类型与异步竞态回归。
- 追加本地 AI 路由后，相关 19 项测试通过（7 项界面、12 项路由），覆盖无效输入、认证拒绝、路由选择和错误脱敏；最终完整计数见云端 CI。
- Ruff、导入检查、翻译检查、生产构建、构建体积、版本一致性、发布流程及函数数量检查通过。
- 本地 Chromium 的评分、日线联动、桌面/移动端及可访问性回归共 14 项通过；云端 CI、部署结果将在发布后追加，不能以本地测试替代生产认证和实时服务验证。

## 发布与回滚

通过仓库现有 PR 和 CI 流程发布。生产工作流需 backend、frontend、e2e 全部通过；部署前记录原生产目标，核心路由、私有日线服务或 5xx 检查失败时使用该目标回滚。无数据库迁移。

## 最终生产验收

- 最终前端 56 个文件、802 项测试通过；后端 1,455 项非联网、非慢速测试通过；云端完整浏览器 51 项通过。
- 最终 PR 候选 Lighthouse：性能 98，LCP 1,961 ms，CLS 0，TBT 0；这是固定实验环境，不是实际用户 INP。
- 预览新增 POST 冒烟最初因测试脚本漏带 CSRF 返回 403。脚本现从真实会话接口读取校验值，新增真实中间件回归；没有降低网站的认证要求。
- 正式域名指向 `dpl_4jKpFabwwFNsSLMv82FKbVpKFTM2`，状态 READY，提交 `4db1b48`；生产代码树 `fc7759d11cc642f2c62e05472a01afbfe92775b3` 与最终测试候选一致。
- 正式页面与新版脚本 HTTP 200，评分模块包含验证状态、即时分数和显式资产类型请求。线上已加载新版构建，不能把这一静态资源检查称为已登录页面的视觉检查。
- 生产真实登录后的评分无效请求返回 400，拒绝路径 PASS；没有调用付费模型。
- 私有日线验收通过：发布快照包含 544 个成员，其中 543 个成功，另一个按已有停交易证据处理；这是原有 2026-09-16 快照，并非本次重新采集行情。
- 19 个服务端星标保持不变；认证边界、核心路由和生产日线检查通过。
- 生产工作流近期 5xx 检查通过；Vercel 对该部署最近 10 分钟的 5xx 聚合查询未返回记录。自动回滚步骤未触发。
- 本机存在域名解析/连接问题，使用 Vercel 专用工具及云端 CI 核验生产。Git 传输不稳定时使用官方 GitHub API 同步，逐文件及完整代码树哈希均核对一致；本地与远程提交元数据不同，代码相同。

机器记录见 [production-verification.json](production-verification.json)。本文件中的最终验收记录和 PR 发布说明共同记录实际结果；前轮 README.md 与 acceptance.json 保留当时的研究审计结论。
